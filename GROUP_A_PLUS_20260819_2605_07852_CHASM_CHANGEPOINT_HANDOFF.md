# Group A+ 2026-08-19: arXiv:2605.07852 CHASM Changepoint Detection Pilot

**日期**：2026-08-19（同日的第四篇論文研究，延續同日的2607.03669/2607.00883系列）
**論文**：arXiv:2605.07852 — Khamesi, Cohen, Adams & Bodenham, "CHASM: Online Changepoint Detection in Temporal and Cross-Variable Dependence"
**狀態**：**混合結果——一個非常強的正面案例(COVID)+一個既有機制完全漏掉但CHASM抓到的真實案例(2024-08)，但正式校準門檻後對既有switch觸發日期的整體命中率偏低，closed research_only**

## 1. 論文核心

線上變點偵測方法，監控多變量VAR(1)過程轉換矩陣Θ的特徵值序列（而非重建誤差或單一標量統計量），偵測**動態依賴結構**本身的改變——不只是均值/變異數這種邊際統計量。核心洞察：兩個VAR過程可能邊際變異數完全相同，但動態結構（特徵值/相依方式）完全不同，傳統方法看不出來。方法：遞迴最小平方線上估計Θ_n → 截斷特徵分解取前r個特徵值 → 用最佳線性指派（Hungarian algorithm的一種）對齊連續時間點的特徵值向量避免數值排序不一致 → 追蹤特徵值「瞬時速度」v_n=λ_n-λ_{n-1}，用複數版MEWMA（Mahalanobis距離統計量D²_n）偵測連貫性偏移。完全線上、O(d²r)每步、無分布假設（只要有限矩，對厚尾穩健）。

## 2. 為什麼認為值得測——與Group A+的連結

前兩篇論文（2607.00883）的四軸診斷已經量化證實：Group A+現行regime切換機制（MA gap + drawdown門檻）是**價格層級的落後指標**——實測發現switch_to_defensive事件觸發時drawdown已經先累積5-12%。CHASM提供完全不同性質的訊號：直接監控0050/00631L/00632R彼此之間的**動態依賴關係**有沒有改變，理論上可能比純價格/回撤門檻更早捕捉regime轉換的開始。且**完全不需要新資產類別**（不像2607.00883的選擇權），純粹是對已有3檔資產報酬率做訊號處理，實作門檻低。

## 3. 實作範圍

用純numpy/scipy依照論文Algorithm 1直接實作：
- x_t = (0050日報酬, 00631L日報酬, 00632R日報酬)，d=3
- 已知的00632R 2024-12-02公司行動斷點（跟08-19稍早幾篇論文踩到的同一個）用close-to-close比例回溯調整
- rank r=2、forgetting ρ=1（無遺忘，論文顯示bias漸近消失）、MEWMA學習率α=0.15
- 100天warm-up + 校準期（比照論文自己table 3的grace period慣例）：前100個v_n樣本估計Σ_v/Σ̃_v（Hermitian/pseudo-covariance）後固定，非逐步更新——論文pseudocode本身沒有明確定義Σ_v的線上更新式，這是合理且與論文自己"fixed grace period"慣例一致的簡化
- μ_v設為0（跟論文"under stationary dynamics, v_n fluctuates around zero"一致）

**簡化/未實作的部分**：論文完整版本包含跨session VAR spillover等更複雜設定，這裡只用最基本的VAR(1)在3個資產日報酬率上，沒有嘗試更高階或跨資產類別擴充。

## 4. 第一輪：2020-01-02起，直接跟既知switch事件比對peak時間（探索性，事後找峰值）

**方法瑕疵**：先看D²時序圖，再回頭在每個已知切換日期附近找最大值——這是容易產生後見之明偏誤的做法，已在文件中明確標記。

結果：2021-05-17（提前2天）、2021-07-28（提前12天）看似不錯，但2021-10-07/2022-03-03提前29-38天（間隔太長可疑），2025-03-03完全沒訊號，2026-07-28反而落後6天。**2020-02-18（COVID）因為100天warm-up期正好蓋住而無法測試**——這是最大的缺口，因為COVID是史上最乾淨的單一崩盤測試案例。

## 5. 第二輪：window往前extend到2019-01-02，補測COVID + 正式門檻校準（使用者追問後新增）

### 5.1 COVID測試結果——非常強

```
2020-01-31  4.156
2020-02-03  10.775   <- 全期間(2019-2026, n=1850天)單一最大值
2020-02-04   7.772
2020-02-05   5.657
...(逐日衰減)...
2020-02-18   0.256   <- 既有機制的switch_to_defensive日期
```

**CHASM在2020-02-03示警，比既有switch_to_defensive日期(2020-02-18)早18天，且是整個7.5年測試期間單一最大的統計量值（10.78）**——對應台灣農曆年封關後重新開盤，正是COVID疫情最初從中國傳出、亞股領先反應的真實時間點。這是乾淨、有力的正面案例。

### 5.2 正式門檻校準（用2023年這個確認平靜、全年幾乎無真實切換的年份當校準基準）

閾值 = 2023年D²分布的99百分位數 = 0.1415（2023全年最大值僅0.172，均值0.013——平靜期訊號極乾淨無假警報）。

用這個**預先校準、非事後挑選**的門檻，把全期間（排除校準用的2023年本身）超過門檻的天數collapse成19個警報episode，正式檢查每個已知switch事件前60天內有沒有真的觸發：

| 既有switch事件 | 正式門檻下的結果 |
|---|---|
| **2020-02-18** | **命中，提前18天，peak=10.78（極強）** |
| 2021-05-17 | 未觸發（沒有假警報式的「早期」訊號，第一輪事後挑的peak其實在門檻之下） |
| 2021-07-28 | 未觸發 |
| 2021-10-07 | 未觸發 |
| 2022-03-03 | 邊際命中，提前38天，但peak僅0.218，剛過門檻 |
| 2025-03-03 | 未觸發（確認漏報） |
| 2026-07-28 | 表面上命中但實為誤判（見5.3） |

**正式校準後的命中率遠低於第一輪事後挑峰值給人的印象**——這正是要求正式校準的原因：後見之明比對法會系統性高估訊號品質。7個已知事件裡只有1個強命中（COVID）+1個邊際命中（2022），其餘4-5個是漏報。

### 5.3 意外發現：CHASM抓到既有機制完全漏掉的真實事件

19個警報episode裡，`2024-07-12~2024-08-05`（peak=1.785）落在既有機制**連續兩年多完全沒有觸發防禦切換的空窗期**內（switch事件記錄顯示2022-11-15 switch_to_golden之後，下一次切換是2025-03-03 switch_to_defensive，中間golden狀態維持超過兩年不變）。而2024-08-05是真實發生的全球市場拋售事件（日圓套利交易平倉、日經單日重挫、BOJ升息恐慌），是有廣泛獨立報導佐證的真實壓力事件。**既有MA-gap+drawdown機制對這次事件完全沒有反應，但CHASM獨立抓到了**——這是本次測試裡最有價值的具體發現：不是「CHASM比既有機制更早」，而是「CHASM抓到了既有機制的一個真實盲點」。

同一批警報裡，2022-10-18~11-02（peak=2.16）跟2025-04-08~04-18（peak=2.66，對應川普關稅衝擊）都落在既有機制**已經處於防禦狀態**的期間內，屬於「確認訊號」而非新增資訊。

## 6. 結論

**三層判讀，方向正面但證據不像2607.00883/四軸診斷那樣乾淨：**

1. **假警報率控制良好**：平靜年份（2023）幾乎零雜訊，門檻設定在99百分位時全期間只有19個警報episode，不是雜訊滿天飛的機制。
2. **對最重大的單一衝擊事件（COVID）給出極強、極早的訊號**（提前18天，全期間最大值），這個結果本身相當有說服力。
3. **但對既有switch_to_defensive觸發日期的正式命中率偏低**（7個裡1強命中+1邊際，其餘漏報）——這代表CHASM偵測的「動態依賴結構改變」跟既有機制偵測的「價格/回撤閾值突破」是**兩個部分重疊但不完全相同的現象**，不能直接當作既有機制的提前預警替代品。
4. **最有價值的具體發現**：至少抓到一次（2024-08）既有機制完全沒反應的真實市場壓力事件——顯示這個訊號可能提供的價值不是「讓現有觸發器提前」，而是「補一個現有機制的盲點」，作為**獨立的、平行的壓力確認訊號**而非取代品。

**不建議直接promote或接入production**——單一參數組合(r=2, ρ=1, α=0.15)測試，沒有做參數敏感度、沒有做跨獨立年度的正式F1/precision-recall評估（論文自己的評估標準）、樣本量小（7個已知事件）。若要繼續，下一步應該是：(a) 對rank/forgetting/α做敏感度網格，(b) 正式定義F1-score評估（比照論文Appendix C.1），而不是本次的「事後找peak」或「單一門檻粗篩」，(c) 擴大到更多資產（含00679B或大盤指數^TWII）看訊號會不會更穩定。

## 7. 檔案位置

僅scratchpad，未進repo（研究方向仍在探索階段，訊號強度不足以立即投入正式基礎設施）：
- `chasm.py`——完整CHASM演算法實作（Algorithm 1直接翻譯，含遞迴DMD估計、最佳特徵值對齊、複數MEWMA統計量）
- `chasm_D2_statistic.csv`——2019-01-02~2026-08-18的完整D²統計量時序（最終版本，window往前extend後）

## 8. 待辦 / 下次注意事項

1. 不要重新對「事後找peak」這種比對法照單全收——第一輪的樂觀結果(2021年多次「提前預警」)在正式校準門檻後大多不成立，是重要的方法論教訓。
2. 若要繼續，`chasm.py`已經是完整可重跑的實作，可以直接改參數（RANK/FORGETTING/ALPHA/GRACE）重跑。**但刻意不建議做敏感度網格找更好的參數**——只有7個已知事件當驗證樣本，反覆調參找出「更好」的命中率，正是[[feedback_overfitting_fixed_window_tuning]]警告過的陷阱。
3. **已測試「加入^TWII當第4維」，結果是數值不穩定，不是更好的訊號**（使用者追問「繼續」後新增，並在使用者追問「數值問題?」後做了精確診斷，修正了原本不準確的機制描述）：

   0050跟TWII日報酬相關性高達0.954，幾乎共線性。**原本推測「Γ矩陣趨近奇異、Θ_n發散」，直接驗證後證實是錯的**——實測Γ的條件數全程穩定在230~306、Θ_n頻譜半徑全程遠低於1（最大1.24），估計器本身完全沒有發散。

   真正機制（逐項拆解2025-04-08的D²計算找到）：用於Mahalanobis正規化的4×4擴增共變異數矩陣Σ_zn，最小特徵值只有5.33×10⁻⁹，實質上是奇異矩陣，`pinv()`對這個近乎零的方向做逆運算會把訊號放大約1.88億倍。根因是100天校準期內追蹤到的特徵值速度v_n**幾乎全為純實數**（無虛部）——當v_n恆為實數時，論文用來處理複數的pseudo-covariance Σ̃_v(用v^Tv)會在數學上趨近等於Hermitian covariance Σ_v(用v^Hv)，讓4×4擴增區塊矩陣結構性退化。2025-04-08真實關稅衝擊發生時v_n第一次出現明顯虛部（0.057+0.033j），除以校準期間完全沒見過的近零特徵值方向，直接爆炸。3維版本（0050/00631L/00632R）沒有這個問題，因為三個真正不同資產的動態自然形成健康的複數共軛對，Σ_v/Σ̃_v結構沒有退化。**共線性是誘因，但實際發作機制是「校準期特徵值恰好全為實數→擴增共變異數矩陣結構性奇異」，不是單純的估計器發散**。

   若真的要擴充維度，應該選跟0050相關性低的資產（例如00679B債券），或對輸入做去相關/正交化前處理；若要用高相關性資產，需要額外檢查校準期v_n是否有退化成純實數的情況，或對Σ_zn的求逆加上ridge正規化。
4. 2024-08-05這個「既有機制盲點」的發現值得留意——如果之後有人想幫既有MA-gap+drawdown機制補一個獨立的壓力確認層，這是一個具體的候選方向起點，但目前只有1個獨立驗證案例，遠不足以下任何結論。
