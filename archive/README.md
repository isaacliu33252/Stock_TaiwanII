# Archive

Code or documents that have been explicitly removed from active use belong
here.

Only move files here after production call sites, import sites, and guard tests
confirm they are no longer used by the active strategy path.

