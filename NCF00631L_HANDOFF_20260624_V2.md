# NCF00631L 預測模型 — 交接說明 V2
**日期：2026-06-24**
**涵蓋工作：Task 7 ~ Task 11（本 session 完成）**
**主檔案：`ncf_00631l.py`（1875 行）**

---

## 1. 本 Session 完成的改動（Tasks 7-11）

### Task 7 — Probability Calibration（Platt Sigmoid）

**問題**：HGB / LGB / XGB 等 Boosting 模型輸出極端機率（如 0.006、0.028），拖低 ensemble。

**做法**：
- 手動實作 Platt Scaling：`LogisticRegression(C=1e5).fit(raw_prob.reshape(-1,1), y_calib)`
- 訓練集最後 20% 作為 calibration set（`MIN_CALIB = 60` 最少筆數）
- 加入 `_PlattModel` wrapper class，透過 `predict_proba()` 組合 base model + Platt sigmoid
- 安全機制：`if platt.coef_[0][0] > 0` — 負斜率代表 calibration set distribution shift，回退 raw 機率

**結果**：
| Horizon | 狀態 | HGB Brier | LGB Brier | XGB Brier |
|---------|------|-----------|-----------|-----------|
| H=1 | 7/7 OK | 0.3357 → **0.2461** (-0.0896) | 0.3467 → **0.2451** (-0.1016) | 0.3180 → **0.2465** (-0.0714) |
| H=5 | 7/7 OK | 0.3545 → **0.2578** (-0.0967) | 0.3324 → **0.2480** (-0.0844) | 0.3061 → **0.2491** (-0.0570) |
| H=20 | **0/7 skip(inv)** | — | — | — |

H=20 的 7 個模型全部 Platt inversion（訓練集末段 2023.5-2024 與 val 2025-2026 不同 regime）。接受此限制。

**相關程式碼**：
- `_PlattModel` class（train_classifier 內部）
- `MIN_CALIB = 60` 常數
- `calib_frac=0.20` 參數

---

### Task 8 — Feature Stability Analysis

**做法**：
- `evaluate_feature_stability()` — 5-fold TimeSeriesSplit，計算跨 fold 特徵重要性的 Spearman rank correlation
- `identify_stable_features()` — 3-fold TimeSeriesSplit，找出在所有 fold 都進 top-K 的特徵
- 穩定性等級：A（≥0.85）/ B（≥0.70）/ C（≥0.55）/ D（<0.55）

**結果（`--feature-stability` flag）**：
| Horizon | Grade | Mean Rank Corr | Top-10 Consistency |
|---------|-------|----------------|---------------------|
| H=1 | D | ~0.45 | 低 |
| H=5 | B/C | ~0.65 | 中 |
| H=20 | A | ~0.88 | 高 |

H=1 Grade D 符合預期：短線訊號會隨 regime（COVID / 升息 / 2026 多頭）改變。

**輸出 JSON 新增欄位**：
```json
"feature_stability": {
  "mean_rank_corr": 0.88,
  "stability_grade": "A",
  "top10_consistency": 0.80,
  "n_folds_used": 5
}
```

---

### Task 9 — AUC-based Horizon Weights

**問題**：原本 direction weights 和 return weights 都用 `inv-MAE²`，H=1 佔 83% 但 AUC 僅 0.54。

**做法**：方向預測改用 AUC-based weights：`dir_w_i = max(0, AUC_i - 0.5) / sum`

**結果**：
| | 舊（inv-MAE²）| 新（AUC）|
|-|--------------|---------|
| H=1 | 83% | ~19% |
| H=5 | 14% | ~39% |
| H=20 | 3% | ~42% |

Combined probability 從 0.518（UP）變為 0.410（DOWN）— H=20 的強 DOWN 訊號現在有 42% 權重。

**相關程式碼**（main() horizon ensemble 區段）：
```python
raw_auc_w = np.maximum(0.0, horizon_aucs - 0.5)
dir_w = raw_auc_w / raw_auc_w.sum()          # 方向用 AUC weights
return_w = inv_mae2_w / inv_mae2_w.sum()      # 報酬用 MAE² weights
```

---

### Task 10 — H=20 Stable Feature Sub-model

**動機**：H=20 Platt calibration 完全失效（inversion），但 H=20 特徵穩定性最高（Grade A）。用穩定特徵訓練額外 RF，加入 ensemble。

**做法**：
1. H=20 迴圈前呼叫 `identify_stable_features(X_train_clf, y_train_dir_clf, n_folds=3, top_k=20, min_folds=3)`
2. 14 個穩定特徵（均為趨勢 / 波動率類）：
   `close_ma200_ratio`, `close_ma240_ratio`, `ma20_ma60_ratio`, `ma60_ma200_ratio`,
   `momentum_63`, `volatility_20`, `volatility_60`, `rolling_mdd_60` ...等
3. 訓練 `stable_rf`（RF 500 trees, max_depth=8），加入 AUC-weighted ensemble
4. Inference 改為 per-model feature sets（`clf_sel[nm]["features"]`）

**結果**：
| Regime | stable_rf AUC | Ensemble 變化 |
|--------|---------------|--------------|
| Bull | 0.4933 | weight=0（AUC<0.5，自動排除）|
| Bear | **0.7412** | 0.7052 → **0.7083**（改善） |

**相關程式碼**：
- `identify_stable_features()` 函數
- `train_classifier(stable_features=stable_feats)` 參數
- stable_rf 區段（train_classifier 尾端，`if stable_features and len(stable_features) >= 5:`）
- Inference 改為：`nm_feats = clf_sel[nm]["features"]`

---

### Task 11 — 移除不穩定特徵（Per-horizon Drop）

**做法**：
```python
HORIZON_DROP_FEATURES: dict[int, list[str]] = {
    1: ["close_open_ratio"],
    5: ["rsi_14"],
}
```
在 feature selection 後、regime columns 加回前，自動 drop。

**移除理由**：
- `close_open_ratio`（H=1）：feature stability Grade D，短線 intraday pattern 在不同 regime 不穩定
- `rsi_14`（H=5）：oscillator 類特徵在多頭行情中 regime-specific，不具跨期穩定性

---

## 2. 目前模型架構（V10）

### 2.1 分類模型（7+1 model AUC-weighted ensemble）

| 模型 | H=1 (Bull) AUC | H=5 (Bull) AUC | H=20 (Bear) AUC |
|------|---------------|----------------|-----------------|
| RF | 0.55 | 0.57 | 0.75 |
| ET | 0.55 | 0.60 | 0.63 |
| HGB | 0.51 | 0.57 | 0.58 |
| GB | 0.53 | 0.56 | 0.73 |
| LGB | 0.53 | 0.59 | 0.71 |
| XGB | 0.52 | 0.58 | 0.67 |
| CAT | 0.54 | 0.58 | 0.73 |
| stable_rf（H=20 only）| — | — | 0.74（Bear only）|

### 2.2 Calibration 狀態

| Horizon | 7 models | stable_rf |
|---------|----------|-----------|
| H=1 | 7/7 Platt OK | N/A |
| H=5 | 7/7 Platt OK | N/A |
| H=20 | 0/7 skip(inv) | skip（n_calib<60）|

H=20 calibration 永久失效（regime shift）。若想改善需考慮：
- 縮短 H=20 calibration set 至近 1 年
- 或放棄 Platt，改用 Isotonic Regression

### 2.3 特徵清單（per-horizon）

| Horizon | 移除特徵 | 剩餘特徵數 |
|---------|---------|-----------|
| H=1 | `close_open_ratio` | ~49 |
| H=5 | `rsi_14` | ~49 |
| H=20 | （無移除）stable_rf 用 14 穩定特徵子集 | ~50 |

### 2.4 Horizon Ensemble

```
方向 weights：AUC-based    dir_w = max(0, AUC-0.5) / sum
報酬 weights：MAE²-based   return_w = 1/MAE² / sum
```

典型值（今日）：H1≈19%  H5≈39%  H20≈42%（方向）；H1≈84%（報酬）

---

## 3. 今日預測（2026-06-24，現價 38.08）

```
Horizon      Reg Return  → Price Direction   Prob
H=1           -0.31%   37.96  NEUTRAL   0.463   (6/25)
H=5           -1.56%   37.49       UP   0.591   (7/1)
H=20          -3.85%   36.61     DOWN   0.226   (7/22)

Confidence: 0.42（低，⚠️ 訊號可能不可靠）
Horizon Ensemble: DOWN（combined prob=0.410）
Weighted return: -0.57% → 37.86
```

**方向 AUC weights**：H1=0.18（AUC=0.540）  H5=0.39（AUC=0.587）  H20=0.43（AUC=0.594）

---

## 4. 關鍵限制與已知問題

### 4.1 H=20 Platt Inversion（永久性）
- 訓練集末 20% 的 calibration window（2023.5-2024）與 val set（2025-2026）處於不同 market regime
- 7 個模型全部 slope < 0，calibration 永久 skip
- **影響**：H=20 HGB/LGB/XGB 機率仍然極端（0.006-0.012）
- **緩解**：stable_rf（Bear AUC=0.74）補充訊號，ET（0.6649）在 Bear 權重最高

### 4.2 Bull H=20 方向難以預測
- H=20 Bull ensemble AUC ≈ 0.59（仍算可用）
- stable_rf Bull AUC 0.4933（< 0.5），自動 weight=0
- 根本原因：2x 槓桿 ETF 在多頭市場走勢連續性高，20 日後方向難以捕捉

### 4.3 所有 Horizon R² 為負
- H=1: -0.01, H=5: -0.05, H=20: -0.13
- 方向分類（AUC）比幅度預測（MAE）更有用

---

## 5. 執行指令

```bash
# 標準執行（Tasks 7-11 全部啟用）
python3 ncf_00631l.py --output results/ncf_00631l_YYYYMMDD.json

# 加上 Feature Stability 診斷（較慢）
python3 ncf_00631l.py --feature-stability --output results/ncf_00631l_stability_YYYYMMDD.json

# 加上 Walk-forward 評估
python3 ncf_00631l.py --walk-forward --output results/ncf_00631l_wf_YYYYMMDD.json
```

---

## 6. 輸出 JSON 結構（新增欄位）

```json
{
  "horizons": {
    "1": {
      "classification": {
        "probability_calibration": {
          "method": "platt_sigmoid",
          "n_fit": 462, "n_calib": 115,
          "applied": true, "n_models_calibrated": 7
        },
        "val_auc_raw": 0.551,
        "ensemble_val_brier": 0.244,
        "model_brier": {"rf": 0.250, "et": 0.257, ...},
        "model_brier_raw": {"rf": 0.250, "hgb": 0.336, ...}
      }
    },
    "20": {
      "classification": {
        "stable_submodel": {
          "n_stable_features": 14,
          "bear_auc": 0.7412,
          "bull_auc": 0.4933,
          "bear_ensemble_improvement": 0.0031
        }
      }
    }
  },
  "horizon_ensemble": {
    "direction_weights": {"1": 0.18, "5": 0.39, "20": 0.43},
    "return_weights":    {"1": 0.84, "5": 0.14, "20": 0.03},
    "horizon_aucs":      {"1": 0.540, "5": 0.587, "20": 0.594}
  }
}
```

---

## 7. 未來改善方向

### 高優先
- **H=20 Calibration 修復**：改用 Isotonic Regression（非參數，不假設 sigmoid 形狀），或縮短 calibration window 至近 1 年
- **Bear regime 資料不足**：訓練資料只有 ~90 筆 Bear，考慮引入 0050 or 大盤 Bear 資料做 transfer learning

### 中優先
- **Task 11 擴充**：使用 `--feature-stability` 輸出，自動更新 `HORIZON_DROP_FEATURES`（目前為手動）
- **動態 Horizon Weights**：regime 切換時（Bull→Bear），可能需要即時調整 AUC weights

### 低優先
- **H=20 stable_rf Bull 改善**：嘗試 `min_folds=2`（從 3 放寬），找到更多 Bull 適用的穩定特徵
- **多標的 regime 共享**：用 0050 的 MA200 狀態輔助 H=20 方向判斷

---

## 8. 相關檔案

| 檔案 | 用途 |
|------|------|
| `ncf_00631l.py` | 主程式（1875 行，Tasks 7-11） |
| `results/ncf_00631l_task11_20260624.json` | 本 session 最終輸出 |
| `results/ncf_00631l_auc_horizon_20260624.json` | Task 9 AUC horizon weights 輸出 |
| `results/ncf_00631l_stability_20260624.json` | Task 8 feature stability 輸出 |
| `NCF00631L_HANDOFF_20260624.md` | 前次交接記錄（V1，Tasks 1-6） |
| `NCF00631L_HANDOFF_20260624_V2.md` | **本文件** |

---

## 9. 程式碼位置速查

| 功能 | 位置（行號）|
|------|-----------|
| `HORIZON_DROP_FEATURES` 常數 | ~1388 |
| `identify_stable_features()` | ~930 |
| `evaluate_feature_stability()` | ~960 |
| `_PlattModel` class | train_classifier 內 |
| `MIN_CALIB = 60` | train_classifier 前段 |
| stable_rf 訓練區段 | ~1230-1314 |
| H=20 stable features 呼叫 | ~1487-1492 |
| Per-model feature inference | ~1545-1549 |
| AUC horizon weights | ~1602-1612 |
