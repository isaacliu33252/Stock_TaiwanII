# Handoff

Session handoff notes and migration logs belong here.

Prefer dated subdirectories such as `handoff/2026-07/` for new handoff files.
Existing root-level handoff files are legacy debt and should be moved only in
small verified batches.

