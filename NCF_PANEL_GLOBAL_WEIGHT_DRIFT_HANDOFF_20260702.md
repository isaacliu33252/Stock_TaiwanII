# NCF Panel 全樣本權重漂移（Global Weight Drift）交接紀錄 — 2026-07-02

## 背景

使用者要求驗證是否採用 BayesOpt 建議的 a2118 新參數（h20_max=0.2922, conf_min=0.520,
h5_reentry_min=0.3673，來自 `results/bayesopt_a2118_3d_20260630.json`）。用真正的
`group_a_plus/runners/a2118.py`（而非簡化版機會成本網格腳本）跑 active(0.33/0.55/0.55)
vs BayesOpt shadow 的完整回測，結果兩組參數在最新 panel（`ncf_00631l_panel_latest_20260701.csv`）
上完全一樣：0 次觸發、Sharpe/AnnRet/MaxDD 全部相同。

深入追查後發現：唯一的歷史觸發事件（2025-10-29）在 6/30 panel 裡 confidence=0.5613（兩組門檻都過），
但在只多兩天資料的 7/1 panel 裡 confidence 掉到 0.4635（兩組門檻都過不了）。同一個歷史日期、
理論上特徵不該變，但 confidence 飄動了將近 0.10。

## 根因：Ensemble 權重是全樣本單一權重，非 rolling/walk-forward

`scripts/misc/ncf_00631l.py` 排除了隨機性可能——所有模型都有固定 `random_state=42`/`seed=42`
（RF/ET/HGB/GB/LGB/XGB/CatBoost/TabNet，見 1441/1500/1627-1675/1847/1983/1997 行）。

真正原因在 ensemble 權重公式（~1888-1910 行）：

```python
auc_w   = {name: max(0.0, results[name]["auc"] - 0.5) for name in ALL_NAMES}
brier_w = {name: max(0.0, naive_brier - results[name]["brier"]) for name in ALL_NAMES}
raw_w   = {name: auc_w[name] * brier_w[name] for name in ALL_NAMES}
W       = {name: raw_w[name] / total_w for name in ALL_NAMES}   # 單一一組全域權重
ens_proba = sum(W[name] * results[name]["proba"] for name in ALL_NAMES)  # 套用到整個 panel 每一列
```

`results[name]["auc"]`/`["brier"]` 是用**整個驗證集**（目前固定 `--val-start 2025-01-02`
到「跑的當下最新日期」）算出的單一數字。這組 `W` 接著被套用到 panel 裡**每一天**的預測，
包含 8 個月前的歷史日期。

驗證集每往前推進一天，8 個模型在（略微變大的）整個驗證集上的 AUC/Brier 都會微幅變動，
導致 `W` 重新分配，導致 panel 裡**所有歷史日期**的機率/信心值全部跟著重算一次——即使
那些歷史日期本身的特徵完全沒變。這正是 2025-10-29 confidence 漂移的原因。

## 影響範圍（比 BayesOpt 決策本身更根本）

以下所有依賴「固定歷史 panel 的觸發日期/指標」的分析方法，其實都建立在一個會隨時間漂移的地基上：

- BayesOpt 參數掃描（`bayesopt_a2118_trigger.py`）：優化出的門檻值可能是在對雜訊調參，不只是
  `h5_reentry_min` 這一個維度。
- 機會成本網格（`evaluate_group_a_plus_opportunity_cost.py`）：`trigger_dates` 清單本身不穩定，
  重新產生 panel 後同一組門檻可能算出不同的觸發日期。
- Multiyear walk-forward AUC（`ncf_multiyear_wf.py`）：跨年度 AUC 比較的前提是每年的預測互相獨立，
  但如果用同一份全樣本重訓的 panel，較早年份的預測其實混入了較晚年份資料的權重資訊。
- `strategy.json` 的 `reproducibility_guard_20260701`：已經記錄到同樣現象（"NCF latest panel
  drift was detected"），這次是從 BayesOpt 回測角度獨立驗證到同一個根因。

## 這次順便發現的小 bug（已修正）

`group_a_plus/runners/a2118.py` 和 `group_a_plus/runners/a2111.py` 的 `--end` 參數預設值是
寫死的字串 `"2026-06-25"`（614/615 行 / a2111.py 189/190 行），不會自動抓最新日期。不帶
`--end` 跑回測會silently 少算最近幾天的資料。已在 a2118.py 修正（見下方 commit）。

## 目前結論

1. **BayesOpt 參數不採用**：不是「證據不夠強」，是現在的證據（唯一可比較的觸發事件已隨模型重訓
   消失）直接對調換不出差異，active 參數維持 0.33/0.55/0.55 不變。
2. **架構問題暫不修**：把權重公式改成 rolling/expanding-window（只用「該日期之前」的資料算權重）
   是一個影響範圍很大的改動，會讓所有下游分析（BayesOpt、機會成本、多年WF）的既有結論都需要
   重新驗證。使用者決定先記錄、不立刻動架構。
3. **下次要做這個架構修正時**：建議先用一個小範圍實驗驗證（例如固定某天的 val-end，逐步增加
   訓練/驗證資料，觀察某個歷史日期的 ensemble proba 漂移幅度 vs. 改成 rolling window 後是否
   穩定），再決定要不要全面重寫，因為這牽涉到重新產生所有 `results/ncf_*panel*.csv` 並重跑
   所有依賴它們的下游腳本。

## 相關檔案

| 檔案 | 說明 |
|------|------|
| `results/ncf_00631l_panel_latest_20260630.csv` | 舊 panel，2025-10-29 confidence=0.5613 |
| `results/ncf_00631l_panel_latest_20260701.csv` | 新 panel（多兩天資料），同一天 confidence=0.4635 |
| `results/bayesopt_a2118_3d_20260630.json` | BayesOpt 53 次 probe 結果（決策依據，已否決採用）|
| `GROUP_A_PLUS_FEATURE_SWEEP_HANDOFF_20260630.md` | BayesOpt 原始分析與「先觀察一季」的舊建議 |
| scratchpad（session-local，非正式 results/）| 這次回測比較用的 active/shadow 回測 JSON |

---
*Generated by Claude Code — 2026-07-02*
