# NCF00631L 預測模型 — 交接說明
**日期：2026-06-24**
**版本：V9（最終版）**
**檔案：ncf_00631l.py**

---

## 1. 目標與定位

NCF00631L 是 00631L（2x 槓桿 ETF）的**幅度 + 方向預測系統**，輸出 H=1/H=5/H=20 三個 horizon 的：
- 預期報酬率
- 方向（UP / DOWN / NEUTRAL）
- 機率
- 置信度

**不是交易策略本身**。輸出供 Golden 規則系統（backtest_golden_00631l_v9.py）參考。

---

## 2. 模型架構（V9）

### 2.1 回歸模型（3-model ensemble）

| 模型 | 角色 | 特色 |
|------|------|------|
| RandomForestRegressor | RF | 主力，捕捉非線性關係 |
| ExtraTreesRegressor | ET | 隨機分割，彌補 RF 盲點 |
| ElasticNet | EN (α=0.05) | 線性約束，防止過擬合 |

**移除的模型**：Ridge（長 horizon 失控，H=20 給出 -24%）、HGB/GB（荒謬預測拖累 ensemble）。

**Feature Selection**：預設關閉（`--feature-selection`）。若開啟，threshold=0.065 保留 ~4 features，對 H=1 有幫助但犧牲長 horizon。

### 2.2 分類模型（4-model weighted ensemble）

| 模型 | Walk-forward Acc | 權重 |
|------|-----------------|------|
| HistGradientBoostingClassifier | H=5: 69.9%, H=20: 67.1% | 0.40 |
| RandomForestClassifier | H=5: 61.6%, H=20: 59.6% | 0.30 |
| ExtraTreesClassifier | H=5: 53.0%, H=20: 58.6% | 0.20 |
| MLPClassifier | H=5: 50.0%, H=20: 38.1% ⚠️ | 0.10 |

MLP 權重最低（0.10），H=20 只有 38% 準確率，會拖累但不至于毁掉結果。

### 2.3 Prediction Clipping（保險機制）

| Horizon | Clip Bound |
|---------|-----------|
| H=1 | ±10% |
| H=5 | ±12% |
| H=20 | ±25% |

依據 00631L 歷史 5th/95th percentile，V3 預測幾乎從未觸發 clip。

### 2.4 Confidence Signal

根據三個組件計算 0-1 置信度分數：

| 組件 | 權重 | 計算方式 |
|------|------|---------|
| 共識（consensus）| 40% | 三個 horizon 中贏家的比例（1.0=全數相同）|
| 幅度（magnitude）| 40% | `|raw_prob - 0.5| * 2` |
| 分散度（spread）| 20% | `max(0, 1 - std(probabilities) * 4)` |

**收縮公式**：`calibrated = 0.5 + (raw - 0.5) * (0.3 + 0.7 * confidence)`

- confidence 高 → shrinkage 低 → 機率維持原樣
- confidence 低 → shrinkage 高 → 機率向 0.5 收縮

**行動規則**：
- confidence < 0.4 → 顯示警告（unreliable）
- confidence < 0.3 且方向 DOWN → 改為 NEUTRAL

### 2.5 Horizon Ensemble

使用 `inv-MAE²` 權重（更精確的 horizon 權重更高）：

| Horizon | 權重 |
|---------|------|
| H=1 | ~0.84 |
| H=5 | ~0.14 |
| H=20 | ~0.03 |

---

## 3. 版本演進

| 版本 | 改動 | H=1 | H=5 | H=20 | Final Ens |
|------|------|-----|-----|------|-----------|
| Baseline | HGB+GB（原始）| -2.23% | -4.29% | -16.90% | -2.79% |
| V2 | 4-model（+Ridge+EN）| -1.10% | -3.49% | -9.25% | -1.62% |
| V3 | 移除 Ridge，3-model | **-0.72%** | **-1.32%** | **-4.37%** | **-0.90%** |
| V5 | V3 + FS（22 feat）| -0.45% | -1.77% | -4.53% | -0.73% |
| V8 | V3 + weighted clf | -0.72% | -1.32% | -4.37% | -0.90% |
| **V9** | **V8 + confidence** | **-0.72%** | **-1.32%** | **-4.37%** | **-0.90%** |

**最大改善**：移除 Ridge（V2 → V3），H=20：-9.25% → -4.37%（+4.88pp）

**Feature Selection**（V5）：對 H=1 有幫助（-0.72%→-0.45%），但犧牲長 horizon，不建議。

---

## 4. 目前結果（2026-06-23，close=40.20）

```
Horizon      Reg Return  → Price Direction   Prob
H=1           -0.72%   39.91     DOWN   0.216
H=5           -1.32%   39.67       UP   0.507
H=20          -4.37%   38.44     DOWN   0.128

Confidence: 0.534（中等）
Horizon Ensemble: DOWN（combined prob=0.254）
Weighted return: -0.90% → 39.84
```

---

## 5. 關鍵診斷數字

| Metric | H=1 | H=5 | H=20 |
|--------|-----|-----|------|
| Val MAE | 2.33% | 5.76% | 12.94% |
| Val R² | -0.02 | -0.11 | -0.13 |
| Reg Bull MAE | 0.129 | — | — |
| Reg Bear MAE | 0.120 | — | — |
| Clf Bull Acc | 52%（RF）| — | — |
| Clf Bear Acc | 67%（RF）| — | — |

---

## 6. 瓶頸與限制

1. **方向分類（最大瓶頸）**：Bull market 方向分類 Acc 只有 52%（幾乎隨機）。主因：多頭市場走勢連續性高，模型容易过度自信。
2. **R² 為負**：所有 horizon 的 R² 都是負的，表示模型預測不如簡單平均值。方向分類比幅度預測更有用。
3. **H=20 預測最差**：MAE 12.94%，占 horizon ensemble 權重只有 3%，影響有限但仍是瓶頸。
4. **00631L 是 2x 槓桿 ETF**：H=20 的真實波動極大（歷史 min/max = -48.6% / +56.4%），任何模型都很難準確。

---

## 7. 未來改善方向（建議）

### 高優先
- **方向分類器替換 MLP**：MLP 在 H=20 只有 38% Acc，考慮換成 GradientBoostingClassifier 或增加 ET 權重
- **Bear regime 學習不足**：訓練資料只有 90 筆 Bear（2015-2020 資料），Bear Acc 67% 但數據太少

### 中優先
- **Walk-forward weighting 動態調整**：用即時 WF 準確率調整 classification weights，而非固定
- **Regime-aware horizon weights**：Bull 時多靠 H=1，震盪時多靠 H=20

### 低優先
- **動態 Clip bounds**：根據 ATR 或波動率指數調整 clip 範圍
- **多標的 regime 共享**：用 0050 或大盤的 regime 判斷輔助 00631L

---

## 8. 執行方式

```bash
# 標準執行（V9，最終版）
python3 ncf_00631l.py --output results/ncf_00631l_v9.json

# 加上 Walk-forward 診斷
python3 ncf_00631l.py --walk-forward --output results/ncf_00631l_v9_wf.json

# Feature Selection（實驗性，不建議常規使用）
python3 ncf_00631l.py --feature-selection --output results/ncf_00631l_v5.json
```

---

## 9. 相關檔案

| 檔案 | 用途 |
|------|------|
| `ncf_00631l.py` | **主程式（V9）** |
| `backtest_golden_00631l_v9.py` | Golden 規則回測（V9 MA50 hybrid）|
| `backtest_golden_00631l_v13.py` | ATR scaling 版 |
| `results/ncf_00631l_v9.json` | V9 最新輸出 |
| `FinRL/data/stock_data.db` | 00631L 歷史資料（DuckDB，2061筆）|

---

## 10. 快速參考

```
回歸：RF + ET + ElasticNet(α=0.05)
分類：HGB(40%) + RF(30%) + ET(20%) + MLP(10%)
Clipping：H=1 ±10%, H=5 ±12%, H=20 ±25%
Confidence信號：有（0.1~1.0，<0.4警告，<0.3 DOWN→NEUTRAL）
Feature Selection：關閉（threshold=0.065可實驗）
訓練區間：2020-01-01 ~ 2024-01-01（976筆）
驗證區間：2024-01-01 ~ 2026-06-23（334筆）
```
