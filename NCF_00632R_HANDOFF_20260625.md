# NCF_00632R 交接記錄 — 2026-06-25

## 1. 本 Session 完成的工作

| 任務 | 狀態 |
|------|------|
| 建立 ncf_00632r.py（仿 ncf_00631l.py 架構） | ✅ |
| 基準訓練（2015~2024 train, 2025~2026-06-24 val） | ✅ |
| 參數網格搜索（direction_threshold × tbl_mult） | ✅ |
| 套用最佳參數並完整驗證 | ✅ |
| WSL2 記憶體耗盡問題診斷與修復 | ✅ |

---

## 2. 檔案位置

| 檔案 | 說明 |
|------|------|
| `ncf_00632r.py` | 主策略模組（tuned defaults 已套用） |
| `sweep_ncf_00632r.py` | 參數掃描工具（RF+ET only, n=150） |
| `results/ncf_00632r_train2015_2024_backtest2025_20260624.json` | Base 結果（thr=0.005, tbl=0.75） |
| `results/ncf_00632r_tuned_thr000_tbl10.json` | **最終 Tuned 結果**（thr=0.0, tbl=1.0） |
| `results/ncf_00632r_sweep_summary.json` | 9 組掃描摘要與決策依據 |

---

## 3. 最終參數（已寫入 ncf_00632r.py defaults）

```python
--direction-threshold  0.0    # 從 0.005 改：保留所有標籤，無中性帶過濾
--tbl-mult             1.0    # 從 0.75 改：00632R 低波動 (1.33%/d) 需較寬 ATR 邊界
n_jobs                 2      # 從 -1 改：WSL2 記憶體限制 (11 GB)
thread_count           2      # 同上
```

---

## 4. 驗證結果

### 4a. 全期 Val AUC（2025-01-02 ~ 2026-06-24）

| Horizon | Base AUC | Tuned AUC | Δ |
|---------|----------|-----------|---|
| H=1 (1日) | 0.5404 | **0.5492** | +0.009 |
| H=5 (5日) | 0.6099 | **0.6360** | +0.026 ★ |
| H=20 (20日) | 0.7939 | **0.7970** | +0.003 |

H=20 Bear-only：thr=0.005 → 0.7496 vs thr=0.0 → **0.7795**（+0.030）

### 4b. Walk-Forward 準確率（5 視窗平均）

| Model | H=1 Base | H=1 Tuned |
|-------|----------|-----------|
| RF | 0.5455 | 0.5455 |
| ET | 0.5549 | 0.5549 |
| HGB | 0.5589 | 0.5548 |
| GB | 0.5567 | 0.5593 |

### 4c. 當日預測（2026-06-24）

```
最新收盤：10.04（2026-06-24）
當前 Regime：BEAR（00632R 自身 MA200 以下）

H=1：DOWN  prob_up=0.3947  ret_pred=-3.92%
H=5：DOWN  prob_up=0.4220  ret_pred=+10.00%（上限截斷）
H=20：UP   prob_up=0.8456  ret_pred=+7.90%

Ensemble：UP  calibrated_prob_up=0.6164  confidence=0.4116
```

### 4d. 重要特徵（Top 10）

```
short_chg_pct          0.1016  ← 融券變化率（最關鍵）
margin_short_log       0.0865  ← 融券餘額對數
inst_foreign_net       0.0585  ← 外資淨買賣
atr_14_normalized      0.0497
vix                    0.0405
rsi_28                 0.0295
rsi_14                 0.0257
twii_vs_ma20           0.0244
gap                    0.0237
return_21d             0.0234
```

---

## 5. 00632R 特有設計

### EXT 特徵（抓取 0050 趨勢作為反向 ETF 核心驅動力）

```python
# 在 load_external_df() 中：
et50_ma200 = et50_s.rolling(200).mean()
ext["eti0050_ma200_regime"] = (et50_s > et50_ma200).astype(float)  # shift=0
ext["eti0050_ma200_gap"]    = et50_s / et50_ma200 - 1              # 連續版
```

### CLIP_BOUNDS（比 00631L 保守）

```python
CLIP_BOUNDS = {1: (-0.05, 0.05), 5: (-0.10, 0.10), 20: (-0.15, 0.15)}
```

### HORIZON_DROP_FEATURES（main() 局部變數，不可從模組層級 import）

```python
HORIZON_DROP_FEATURES = {1: ["close_open_ratio"], 5: ["rsi_14"]}
```

---

## 6. 日常使用方式

```bash
# 每日預測（使用 tuned defaults）
python3 ncf_00632r.py --output results/ncf_00632r_$(date +%Y%m%d).json

# 含 Walk-Forward（較慢，每週更新一次即可）
python3 ncf_00632r.py --walk-forward --output results/ncf_00632r_wf_$(date +%Y%m%d).json

# 重新訓練（若需更新模型）
python3 ncf_00632r.py \
  --train-start 2015-01-01 \
  --val-start 2025-01-02 \
  --val-end 2026-06-24 \
  --walk-forward \
  --output results/ncf_00632r_retrain_$(date +%Y%m%d).json
```

---

## 7. 已知問題與解法

### WSL2 背景 Job 殭屍 Process
**問題**：Claude Code 背景 job 回報 completed，但 Python process 仍在執行，可積累至 10+ GB RAM  
**診斷**：`ps aux | grep "ncf_00632"` → 查看殘留 process  
**修復**：`pkill -f "ncf_00632r.py" && pkill -f "sweep_ncf_00632r.py"`

### direction_threshold 中間值反效果
thr=0.002 比 thr=0.005 **更差**（H=1 AUC: 0.5162 → 0.5084）。  
原因：介於「有用的中性帶」和「無過濾」之間，增加邊界噪聲標籤。  
結論：採用 0.0（完全二元）或較大值（≥0.005），避免中間值。

### SIGPIPE（使用管道時 Python 被中斷）
**錯誤**：`python3 ncf_00632r.py ... | grep "..."` 導致存檔前中斷  
**修復**：改用完整指令不帶管道

---

## 8. 可能的後續任務

1. **ncf_00632r + ncf_00631l 信號整合**：兩者信號對沖驗證（00632R DOWN ↔ 00631L UP 是否一致）
2. **Group A+ 決策整合**：將反向 ETF 預測納入多元信號
3. **資料庫更新**：外資/融資資料目前落後 6-7 天，需定期執行 `update_db.py`
4. **定期重訓**：建議每季（3個月）以最新數據重新訓練

---

*生成日期：2026-06-25 | 訓練期：2015-01-01 ~ 2024-12-31 | 驗證期：2025-01-02 ~ 2026-06-24*
